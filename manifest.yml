---
applications:
  - name: opre-ops-test
    memory: 700M
    buildpack: python_buildpack
    path: ./backend/
    env:
      PYTHONPATH: ./
    # TODO:switch over to a production-ready webserver like gunicorn
    command: cd opre_ops && python manage.py migrate && python manage.py runserver 0.0.0.0:8080
  - name: opre-ops-frontend-test
    memory: 64M
    buildpack: https://github.com/cloudfoundry/staticfile-buildpack
    path: ./frontend/build/

services:
 - opre-ops-psql-db
