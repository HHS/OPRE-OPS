import { create } from "vest";

/**
 * Validation suite for Request Pre-Award Approval page
 * Note: No frontend validation needed - notes field is optional
 * Backend handles all authorization and required field validation
 */
// eslint-disable-next-line no-unused-vars
const suite = create((data = {}) => {
    // Notes field is optional - no frontend validation needed
    // Backend handles authorization and required field validation
});

export default suite;
